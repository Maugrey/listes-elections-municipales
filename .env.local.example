# Développement local (PostgreSQL local, sans mot de passe)
DATABASE_URL=postgresql://postgres@localhost:5432/elections_municipales

# Production (Neon TCP — récupérer dans le dashboard Neon → Connection string)
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
